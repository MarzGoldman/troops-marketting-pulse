{
  "name": "@tmp/redirect-service",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "prestart": "pnpm -w prisma migrate deploy --schema=packages/db/prisma/schema.prisma && pnpm -w prisma generate --schema=packages/db/prisma/schema.prisma",
    "start": "NODE_ENV=production node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^8.5.0",
    "@prisma/client": "^6.16.2",
    "dotenv": "^17.2.2",
    "fastify": "^4.29.1",
    "ua-parser-js": "^2.0.5"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^5.6.2"
  }
}